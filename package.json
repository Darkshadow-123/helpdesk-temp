{
  "name": "capacity-helpdesk",
  "version": "1.0.0",
  "type": "module",
  "description": "Capacity Helpdesk - MERN Stack Application",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "npm install && cd client && npm install --include=dev && npm run build",
    "start": "node server/index.js",
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && node index.js",
    "client:dev": "cd client && npx vite"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
